---
# Issue Let's Encrypt certificates for production domains only.
- name: Generate SSL certificates with certbot
  hosts: local
  become: yes
  vars_files:
    - "{{ inventory_dir }}/../group_vars/all.yml"
    - "{{ inventory_dir }}/../group_vars/production.yml"
  roles:
    - ssl
