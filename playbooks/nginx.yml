---
# Generate nginx vhosts for production domains (experimental project excluded).
- name: Configure nginx vhosts
  hosts: local
  become: yes
  vars_files:
    - "{{ inventory_dir }}/../group_vars/all.yml"
    - "{{ inventory_dir }}/../group_vars/production.yml"
  roles:
    - nginx
